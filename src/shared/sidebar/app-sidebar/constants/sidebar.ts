import { Home, LayoutDashboard, Activity, Settings, Bell, type LucideIcon } from "lucide-react";

export const staticIcons: Record<string, LucideIcon> = {
  Home,
  LayoutDashboard,
  Activity,
  Settings,
  Bell,
};
